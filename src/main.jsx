import React from 'react';
import ReactDOM from 'react-dom/client';
import { GifHubApp } from './GifHubApp';
import './styles.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GifHubApp />
  </React.StrictMode>
);